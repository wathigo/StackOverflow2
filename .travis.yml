language: python

python:
- "3.6.7"

install:
  - pip install -r requirements.txt
  - pip install coveralls
  
script:
  - pytest --cov=./app

branches:
  only:
    - develop

after_success:
   - coverall
